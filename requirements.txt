# Core pipeline
numpy>=1.24
scipy>=1.10
rasterio>=1.3
shapely>=2.0        # 2.0 required: STRtree returns indices, set_precision exists
pyshp>=2.3
affine>=2.4

# Optional: classify.py --preview writes a PNG of the label raster
pillow>=10.0
